{
  "name": "urgent-email",
  "version": "1.0.0",
  "description": "AI-powered email monitoring that flags urgent emails via Telegram",
  "main": "api/check-emails.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "dotenv": "^17.0.0",
    "googleapis": "^128.0.0",
    "telegraf": "^4.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/node": "^3.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
